import { test, expect } from '@playwright/test';

const Users = ['standard_user', 'locked_out_user', 'problem_out_user', 'preformance_glitch_user', 'error_user', 'visual_user'];

for (let i=1; i <=6; i++){
    function test
}
;